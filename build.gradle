allprojects {
    apply plugin: 'maven-publish'

    group = 'com.unblu.tools'
    version = '1.0.0-SNAPSHOT'
}

subprojects {
    ext {
        junitVersion = '5.3.1'
        logbackVersion = '1.2.3'
    }

    repositories {
        mavenCentral()
        jcenter()
    }
}